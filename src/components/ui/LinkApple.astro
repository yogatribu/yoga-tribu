---
interface Props {
  href: string;
  external?: boolean;
  tone?: 'ocra' | 'light';
}
const { href, external = false, tone = 'ocra' } = Astro.props;
---

<a
  href={href}
  class={`link-apple link-apple--${tone}`}
  {...(external ? { target: '_blank', rel: 'noopener noreferrer' } : {})}
>
  <span class="link-text"><slot /></span>
  <span class="link-arrow">&rsaquo;</span>
</a>

<style>
.link-apple {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  text-decoration: none;
  font-family: Inter, sans-serif;
  font-size: 14px;
  font-weight: 400;
  cursor: pointer;
}
.link-apple--ocra { color: #9c9a66; }
.link-apple--light {
  color: rgba(255, 255, 255, 0.8);
  transition: color 100ms ease-out;
}
.link-apple--light:hover { color: rgba(255, 255, 255, 1); }

.link-apple .link-text {
  text-decoration: underline;
  text-underline-offset: 2px;
  text-decoration-color: transparent;
  transition: text-decoration-color 100ms ease-out;
}
.link-apple:hover .link-text {
  text-decoration-color: currentColor;
}
.link-apple .link-arrow {
  text-decoration: none !important;
}
</style>
